# 接入邮箱登录（Email Code Login）方案

> 状态：方案评审稿（待实现）
> 日期：2026-08-10
> 关联系统：现有「手机短信验证码」登录/注册（`/api/auth/send-code` → `/api/auth/login` / `/api/auth/register`）

---

## 1. 目标与范围

在现有「手机号 + 短信验证码」登录/注册能力之外，新增平行的「邮箱 + 邮件验证码」能力，界面与交互保持与手机验证码登录**完全一致**（同一个认证弹窗、同一个"获取"按钮、同样的验证码输入与倒计时逻辑）。

**已确认的范围边界：**

| 项 | 决定 |
|---|---|
| 账号身份模型 | **同账号双标识**：一个账号可同时绑定手机号与邮箱，复用现有 `user_identities` 机制（`provider='email'`）。注册时二选一，登录时两者都能用。 |
| 邮件发送服务 | **先 mock 后接真实**：仿照短信做法，先实现 mock（开发态回显验证码）+ 抽象层 `email-provider.ts`，用环境变量切换真实通道（SMTP / Resend 等），零成本先跑通全流程。 |
| 覆盖范围 | **仅普通用户的注册 + 登录**。管理员保持现有「账号密码 + 手机号管理员验证码」不变；「忘记密码」本次仍走手机号。 |

**不做的事（本期）：** 管理员邮箱验证码、忘记密码走邮箱、手机号账号在设置页补绑邮箱（数据模型已预留，可在后续迭代补上）。

---

## 2. 方案总览

```
                认证弹窗 (auth-modal.tsx)
   ┌──────────────────────────────────────────────┐
   │  [手机号] / [邮箱]  ← 新增切换开关（同屏）      │
   │  输入框(手机:+86 / 邮箱:无前缀)                │
   │  [验证码] [获取] 60s 倒计时                    │
   └──────────────────────────────────────────────┘
            │  fetch /api/auth/send-code
            │  { purpose, phone } 或 { purpose, email }
            ▼
   ┌──────────────────────────────────────────────┐
   │  send-code/route.ts                           │
   │   ├─ 手机号分支（现状，不动）                   │
   │   └─ 邮箱分支（新增）                          │
   │        validateEmail → getUserByEmail 业务校验 │
   │        createVerificationCode({email,...})     │
   │        sendVerificationEmail({email,code})     │
   │             └─ email-provider.ts (mock/smtp)   │
   └──────────────────────────────────────────────┘
            │  fetch /api/auth/login {mode:'code', email, code}
            │        或 /api/auth/register {username, email, password, code}
            ▼
   ┌──────────────────────────────────────────────┐
   │  lib/server/db.ts                             │
   │   resolveEmailCodeLogin / loginWithEmailCode   │
   │   registerUser(支持 email) / createEmailOnlyUser│
   │   validateVerificationCode(支持 email 查找)     │
   │   users.email 唯一约束 + user_identities(email) │
   └──────────────────────────────────────────────┘
```

核心思路：**尽量复用现有端点和函数**，把 `phone` 这一查找键抽象成「联系方式 = phone | email」二选一，避免另起一套认证体系。

---

## 3. 数据层设计

### 3.1 `users` 表（已存在，需补强）
- 已有 `email TEXT NOT NULL` 列，但目前只是自动生成的 `${username}@local` 占位，**无唯一约束、无按邮箱查用户的函数**。
- 本期：
  - 新增应用层唯一性校验（注册/邮箱登录自动建号前调用 `getUserByEmail` 判重）。
  - 可选：加唯一索引 `CREATE UNIQUE INDEX IF NOT EXISTS idx_users_email ON users(email)`。
    - **迁移安全说明**：现有占位邮箱为 `admin@local`、`admin-16698604646@local`、以及注册时生成的 `${username}@local`（因 username 唯一而天然唯一），与真实邮箱（含 `@` 且非 `local` 域名）不会冲突。上线前用脚本确认无重复真实邮箱即可加索引；若担心风险，可仅保留应用层校验。

### 3.2 `verification_codes` 表（需加列）
现有表以 `phone` 为查找键。新增一列，使同一张表同时承载手机/邮箱验证码：
```sql
ALTER TABLE verification_codes ADD COLUMN email TEXT NOT NULL DEFAULT '';
```
- 一行验证码要么有 `phone`、要么有 `email`（不会同时为两者）。
- 现有查询、限流、消费逻辑统一改为「按传入的 phone 或 email 查找」。

### 3.3 `user_identities` 表（已支持，无需改结构）
复用现有机制，邮箱登录/注册成功后写入：
```ts
ensureUserIdentity(user.id, "email", email)  // provider='email', provider_user_id=email
```
与现有 `provider='phone' / 'password' / 'wechat'` 并列。

---

## 4. 后端改动（`lib/server/db.ts`）

新增/改造函数（保持与手机号系列同名对称）：

| 函数 | 说明 |
|---|---|
| `normalizeEmail(value)` | trim + 转小写 + 校验 `^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$`，非法抛 `INVALID_EMAIL_MESSAGE`。 |
| `getUserByEmail(email)` | 按 `users.email` 查用户，返回 `AuthUser \| null`（对标 `getUserByPhone`）。 |
| `createVerificationCode(input)` | `input` 增加可选 `email`。有 `email` 时走 `normalizeEmail`，不走 `normalizePhone`；写入 `email` 列；限流键用 email。 |
| `validateVerificationCode` / `consumeVerificationCode` / `verifyVerificationCode` | 入参增加「`phone` 或 `email`」二选一；`WHERE` 子句相应改为 `phone = ?` 或 `email = ?`。哈希比对、`attempt_count>=5` 锁死、`timingSafeEqual` 逻辑原样复用。 |
| `assertVerificationRateLimit(target, purpose, ip)` | `target` 为 `{phone}` 或 `{email}`；冷却/日限额逻辑不变（60s 冷却、单目标日≤10、单 IP 日≤30）。 |
| `resolveEmailCodeLogin({ email, code, bindRequired })` | 对标 `resolvePhoneCodeLogin`：已存在邮箱用户→校验登录；不存在且 `bindRequired`→返回 `requiresBinding`；不存在且非绑定→自动建号（`createEmailOnlyUser`）+ 登录。 |
| `createEmailOnlyUser(email, source)` | 对标 `createPhoneOnlyUser`：用邮箱本地名派生唯一 `username`（如 `user_xxxx` 或邮箱前缀 + 随机后缀，确保不与现有 username 冲突），`email` 写入真实值，`phone` 留空，套餐 `free`，写欢迎站内信。 |
| `loginWithEmailCode({ email, code, purpose })` | 对标 `loginWithPhoneCode`。 |
| `registerUser(input)` | `input` 增加可选 `email`，与 `phone` 二选一；`ensureUniqueUser` 同步校验 email 唯一；写 `users.email` 真实值；写 `user_identities(email)`。 |
| `ensureUniqueUser(username, phone?, email?)` | 增加 email 唯一性校验：`SELECT ... WHERE lower(email)=lower(?)`。 |

**关键安全点（与短信一致）：** 验证码明文不落库（只存 `code_hash=hash(target:purpose:code)`）、IP/UA 仅存 hash、限流 + 错误次数锁死、常量时间比较。

---

## 5. 邮件发送能力（新增 `lib/server/email-provider.ts`）

完全对标 `sms-provider.ts` 的结构与 mock 行为：

```ts
export type EmailDeliveryResult = {
  provider: "smtp" | "resend" | "mock"
  requestId: string
  devCode?: string
}

export async function sendVerificationEmail(input: {
  email: string
  code: string
  purpose: "login" | "register"  // 本期仅这两种
}): Promise<EmailDeliveryResult>
```

- **provider 选择**：读 `process.env.EMAIL_PROVIDER`；为空且在非生产且无任何真实配置时 → `mock`。
- **mock 行为**：返回 `{ provider:"mock", requestId, devCode: code }`（仅非生产且未强制关闭时回传 `devCode`，与短信 `shouldUseMockSms` 一致），前端据此在界面回显「本地测试验证码：xxxx」。
- **smtp（后续）**：用 `nodemailer` 直连，读 `SMTP_HOST/PORT/USER/PASS/SECURE` + 发件人 `EMAIL_FROM`；中文验证码邮件模板按 purpose 区分文案。
- **resend（后续）**：读 `RESEND_API_KEY` + `RESEND_FROM`，调用 `https://api.resend.com/emails`。
- 邮件模板：标题/正文含品牌名、验证码（等宽大字）、10 分钟有效期提示、用途说明（登录/注册）。

> 依赖：接 SMTP 时需 `npm i nodemailer`（mock 阶段不需要）。

---

## 6. API 路由改动

### 6.1 `app/api/auth/send-code/route.ts`（增量）
- 解析 `body.email`：若 `email` 存在则进入**邮箱分支**，否则走现有手机分支（完全不动手机逻辑）。
- 邮箱分支：
  1. `normalizeEmail` 校验格式；非法 → `400 INVALID_EMAIL`。
  2. 业务前置校验（对标手机）：
     - `purpose==="register"` 且 `getUserByEmail` 已存在 → `409 EMAIL_ALREADY_REGISTERED`（含 username）。
     - `purpose==="login"` / `register` 命中管理员邮箱 → 抛错（管理员不走邮箱）。
  3. `createVerificationCode({ email, purpose, ip, ua })`。
  4. `sendVerificationEmail({ email, code, purpose })`；成功 `markVerificationCodeSent`，失败 `markVerificationCodeFailed`。
  5. 返回 `{ ok:true, expiresAt, devCode }`（devCode 仅 mock）。
- `admin` purpose 严格保持手机路径，不接受 email。

### 6.2 `app/api/auth/login/route.ts`（增量）
- `mode==="code"` 时，若 `body.email` 存在 → 调 `resolveEmailCodeLogin({ email, code, bindRequired })`；返回逻辑与手机一致（`requiresBinding` / `attachSession`）。

### 6.3 `app/api/auth/register/route.ts`（增量）
- 请求体允许 `email`（与 `phone` 二选一）。校验：有 email 则 `normalizeEmail`，调 `registerUser({ username, email, password, code, purpose })`。手机分支行为不变。

---

## 7. 前端改动（`components/auth-modal.tsx`）

目标：**界面与手机验证码登录相同**，仅在短信相关步骤顶部加一个「手机号 / 邮箱」切换，输入与"获取"逻辑复用。

### 7.1 状态与文案
- 新增状态：`email: string`、`contactMethod: "phone" | "email"`（默认 `phone`，保持现状不打扰老用户）。
- 文案表 `authCopy` 增中/英：
  - `email: "邮箱" / "Email"`、`emailPlaceholder`、`invalidEmail`、`codeSentToEmail`、`codeSentToEmailDev(code)`。
  - 步骤标题可保持不变（如「手机验证码登录/注册」通用化为「验证码登录/注册」可选，但不强制）。

### 7.2 通用化现有组件
- 将 `PhoneCodeFields` 抽象为 `ContactCodeFields`，新增 `method: "phone" | "email"` 入参：
  - `method==="phone"`：渲染 `+86` 前缀 + `phoneDigits` + 手机号校验（现状）。
  - `method==="email"`：无前缀，普通文本输入，`inputMode="email"`，校验邮箱格式；标签显示「邮箱」。
- 新增 `requireValidContact()`：按 `contactMethod` 调 `isMainlandMobile(phone)` 或邮箱正则，复用现有 `invalidPhone`/`invalidEmail` 反馈。

### 7.3 切换开关
- 在 `sms`、`phoneRegister`、`bind` 三个桌面/移动步骤的表单顶部加一组分段控件「手机号 | 邮箱」，切换 `contactMethod` 时清空对方字段与验证码状态（复用 `openDesktopStep`/`openMobileStep` 的清理逻辑）。

### 7.4 提交逻辑
- `sendCode(purpose)`：`payload` 从 `{ purpose, phone }` 改为 `contactMethod==="email" ? { purpose, email } : { purpose, phone }`。
- `submitSmsLogin(bindRequired)`：手机走 `resolvePhoneCodeLogin` 入参；邮箱走 `resolveEmailCodeLogin` 入参（仅替换字段与后端调用）。
- `submitRegister(purpose)`：注册 body 由 `{ username, phone, ... }` 改为按 `contactMethod` 传 `email` 或 `phone`。
- `submitResetPassword`：本期保持手机（不加 email 分支），避免超出范围。
- 反馈文案：`codeSent` / `codeSentDev` 已能复用；邮箱场景用 `codeSentToEmail` 系列（或复用 `codeSent` 亦可，文案待定）。

> 注意：本项目**不使用 Tailwind**，`auth-modal` 样式在 `app/globals.css`。新增切换控件沿用现有 `.auth-template-*` 类，不要引入 Tailwind 工具类。

---

## 8. 文件改动清单

| 文件 | 改动类型 | 内容 |
|---|---|---|
| `lib/server/db.ts` | 改/增 | `normalizeEmail`、`getUserByEmail`、`createVerificationCode`（支持 email）、`validate/consume/verifyVerificationCode`（email 查找）、`assertVerificationRateLimit`（target 抽象）、`resolveEmailCodeLogin`、`createEmailOnlyUser`、`loginWithEmailCode`、`registerUser`（email）、`ensureUniqueUser`（email）、`verification_codes.email` 列迁移、可选 `idx_users_email` 唯一索引。 |
| `lib/server/email-provider.ts` | 新增 | 邮件发送抽象层（mock / smtp / resend），对标 `sms-provider.ts`。 |
| `app/api/auth/send-code/route.ts` | 改 | 新增 email 分支（业务校验 + 建码 + 发信）。 |
| `app/api/auth/login/route.ts` | 改 | `mode:"code"` 支持 `email` 调 `resolveEmailCodeLogin`。 |
| `app/api/auth/register/route.ts` | 改 | 支持 `email` 注册。 |
| `components/auth-modal.tsx` | 改 | `contactMethod` 切换、`ContactCodeFields`、`requireValidContact`、email 文案、sendCode/submit 改造。 |
| `app/globals.css` | 改（小） | 邮箱/手机切换控件样式（复用既有类）。 |
| `package.json` | 改（仅接 SMTP 时） | 加 `nodemailer`（mock 阶段不需要）。 |
| `.env.local`（示例） | 改 | 追加 `EMAIL_PROVIDER`、`SMTP_*` / `RESEND_*` 占位说明（留空即 mock）。 |

---

## 9. 实施里程碑

1. **M1 数据 + 发信（mock）**：`db.ts` 邮箱函数 + `verification_codes.email` 迁移 + `email-provider.ts`（仅 mock）。单测覆盖建码/消费/限流/自动建号。
2. **M2 后端邮箱分支**：`send-code` / `login` / `register` 的 email 路径，curl 冒烟跑通（mock 回显 devCode）。
3. **M3 前端切换**：`auth-modal` 手机号/邮箱切换 + `ContactCodeFields` + 文案，本地联调。
4. **M4 真实发信（可选，按排期）**：接 SMTP 或 Resend，配置环境变量，真机收信验证。
5. **M5 测试与回归**：`npm run lint` + 现有冒烟 + 新增邮箱用例（注册/登录/重复邮箱/验证码错误锁死/限流）。

---

## 10. 风险与后续

| 风险 / 待办 | 说明 |
|---|---|
| 自动建号的 username 冲突 | `createEmailOnlyUser` 派生 username 时需查重（`ensureUniqueUser` 已覆盖）；建议前缀 `email_` + 随机，避免与用户自起名撞车。 |
| 已有 `@local` 占位邮箱 | 与真实邮箱域名不冲突；加唯一索引前跑一次去重校验脚本。 |
| 手机号账号补绑邮箱 | 本期不做；数据模型已支持（`users.email` + `user_identities(email)`），后续可在「账号设置」加绑定流程。 |
| 邮箱轰炸 / 滥用 | 复用现有 `assertVerificationRateLimit`（按 email + IP）。若要更强防护，可对接 `docs` 中「四维防御 + 信任阶梯」方案（人机校验 / 缓存去重）。 |
| 管理员账号 | 严格不动；管理员邮箱即使存在也不允许走邮箱验证码登录（发送端与消费端双重拦截）。 |
| 忘记密码 | 本期仍走手机；后续可把 `reset_password` purpose 也扩展 email（复用本方案 `email` 查找逻辑，工作量很小）。 |

---

## 11. 测试要点（验收）

- [ ] 用**未注册邮箱**走登录 → 自动建号并登录成功（session 种下）。
- [ ] 用**已注册邮箱**走登录 → 直接登录。
- [ ] 用邮箱**注册** → 校验 username 唯一、email 唯一、密码强度；成功后可用该邮箱登录。
- [ ] 重复邮箱注册 → `409 EMAIL_ALREADY_REGISTERED`。
- [ ] 邮箱格式非法 → `400 INVALID_EMAIL`。
- [ ] 验证码错误 ≥5 次 → 锁死，提示重新获取。
- [ ] 同邮箱 60s 内重复获取 → 限流提示；单邮箱日≤10、单 IP 日≤30。
- [ ] 验证码 10 分钟过期 → 提示无效或已过期。
- [ ] mock 模式下前端回显「本地测试验证码：xxxx」；生产 mock 关闭时不回显。
- [ ] 切换「手机号/邮箱」时表单状态正确清理，不打扰现有手机登录路径。
- [ ] `npm run lint` 零警告、TypeScript 无报错。

---

## 12. 实现状态（2026-08-11）

**状态：已实现并通过验证。**

已实现文件（相对 `E:\AI-project\car-edit`）：
- `lib/server/db.ts`：迁移加 `verification_codes.email` 列；新增 `normalizeEmail` / `getUserByEmail` / `createVerificationCode({email})` / `resolveEmailCodeLogin` / `loginWithEmailCode` / `createEmailOnlyUser` / `registerUser` 支持 email；`assertVerificationRateLimit` 与 `validateVerificationCode` 按 `(phone OR email)` 限流/校验。
- `lib/server/email-provider.ts`（新增）：mock / Resend(fetch) / SMTP(nodemailer 懒加载) 三态，`sendVerificationEmail` 返回 `devCode` 供本地回显。
- `app/api/auth/send-code/route.ts`：新增 email 分支（格式校验 → `getUserByEmail` 409 → 管理员拦截 → 建码 → 发送 → 状态机）。
- `app/api/auth/login/route.ts`：`mode==="code"` 增加 `email` 分支走 `resolveEmailCodeLogin`。
- `app/api/auth/register/route.ts`：`phone`/`email` 二选一注册，邮箱走 `getUserByEmail` 409。
- `components/auth-modal.tsx`：新增「手机号/邮箱」分段切换、`ContactMethodToggle`、邮箱输入框与文案；桌面/移动端 sms、phoneRegister、bind 步骤均已适配。
- `app/globals.css`：`.auth-template-email`、`.auth-template-method-toggle` 样式。
- `package.json`：新增 `nodemailer` + `@types/nodemailer`（SMTP 支持，可选）。

验收结果：
- `npx tsc --noEmit` 零错误；`next lint` 零警告。
- **隔离 SQLite 运行时测试（18/18 通过）**：覆盖非法邮箱、建码→发送→自动建号登录、大小写不敏感查找、验证码一次性、60s 冷却（发送态）、消费后允许重发、重复邮箱注册拦截、错误≥5 次锁死且正确码亦失效、已有账号 `loginWithEmailCode`、跨邮箱验证码隔离。
  - 关键修复：原限流/校验 SQL 用 `(phone = ? OR email = ?)`，当某路径为空字符串时会误匹配**所有**邮箱-only 行（导致发往 A 邮箱的码会挡住/误校验 B 邮箱）。已改为 `((phone = ? AND phone <> '') OR (email = ? AND email <> ''))`，彻底隔离。

待办（非阻塞）：
- `.env.local` 示例补充 `EMAIL_PROVIDER` / `SMTP_*` / `RESEND_*`（mock 默认即可跑通）。
- 真机联调 UI 流程需本地 `npm run dev`（本沙箱因 safe-delete 守卫无法启动 Next dev server，未做 HTTP 冒烟）。

