# Archived HyperFrames Prototype

Last condensed: 2026-05-29 Asia/Shanghai

This folder is a historical mobile-motion prototype. It is not the current product spec for the Next.js app.

Do not read or edit this folder during normal `car-mod-effect-studio` work. Use current source files instead:

- mobile app: `components/mobile/mobile-studio-app.tsx`
- mobile CSS: `app/globals.css`
- auth: `components/auth-modal.tsx`
- subscription: `components/subscribe-modal.tsx`

Only use this folder if the user explicitly asks to edit or render the HyperFrames prototype.

If that happens, use HyperFrames skills and run:

```bash
npm run check
```
